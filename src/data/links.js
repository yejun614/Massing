/**
 * Where the project lives.
 *
 * Two places link out — the toolbar's GitHub button and the feedback prompt —
 * and a repository that moves would otherwise leave one of them pointing at
 * nothing. One constant, derived once.
 */

export const REPO_URL = 'https://github.com/yejun614/Massing';
export const ISSUES_URL = `${REPO_URL}/issues`;
